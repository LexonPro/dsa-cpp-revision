import React from 'react';
import HeroSection from './components/HeroSection';

/**
 * App — renders the full-screen DSA Tree hero experience.
 * The HeroSection contains the Three.js tree + all HUD overlays.
 */
const App = () => {
  return <HeroSection />;
};

export default App;
